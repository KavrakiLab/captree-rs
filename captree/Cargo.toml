[package]
name = "captree"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
rand = "0.9.1"

[features]
simd = []

[dependencies]
elain = "0.3.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
